<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.raf.projekat2.ognjen_boskovic_rn9518">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".application.FoodRecipeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodRecipes">
        <activity android:name=".presentation.activities.SaveRecipeActivity"></activity>
        <activity android:name=".presentation.activities.SavedRecipeDetailsActivity" />
        <activity android:name=".presentation.activities.RecipeDetailsActivity" />
        <activity android:name=".presentation.activities.MainActivity" />
        <activity android:name=".presentation.activities.LoginActivity" />
        <activity android:name=".presentation.activities.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>